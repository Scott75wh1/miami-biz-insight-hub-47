// This file is now a simple re-export from the new utility files
// Keep it for backward compatibility
import {
  getSearchKeywords,
  getGrowingCategories,
} from './utils';

export {
  getSearchKeywords,
  getGrowingCategories,
};
